name: generate ASCII art work

on:
  push

jobs:
    ascii_job:
        runs-on: ubuntu-latest
        steps:
        - name: checkout repo
          uses: actions/checkout@v4

        - name: List repo files
          run : ls -ltra

        - name: executing ascii art script
          run : |
            chmod +x ascii-script.sh
            ./ascii-script.sh


        # - name: install cowsay binary
        #   run : sudo apt-get install cowsay

        # - name: excute cowsay command
        #   run : cowsay -f dragon "Run for cover, i'm a dragon.... RAWR" >> dragon.txt

        # - name: test file exists
        #   run : grep -i "dragon" dragon.txt

        # - name: read file
        #   run : cat dragon.txt
        
        # - name: List repo files
        #   run : ls -ltra